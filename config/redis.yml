# Redis
default:        &default
  global:       &default_process
    host:       127.0.0.1
    port:       6379
    db:         0
    pool_size:  5
  sinatra:
    <<:         *default_process
  serializer:
    <<:         *default_process
  sidekiq:
    <<:         *default_process
  message_bus:
    <<:         *default_process


#  password:
#  path:
#  connect_timeout: 0.2,
#  read_timeout: 1.0,
#  write_timeout: 0.5

development:
  <<:           *default

test:
  <<:           *default

staging:
  <<:           *default

# Change this to sock on production
production:
  <<:           *default
